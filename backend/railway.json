{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --ignore-scripts"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx prisma db push && node prisma/seed.js && node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
